import LoginForm from "@/app/component/auth/login/LoginForm";

const page = () => {
	return (
		<div className="w-full mx-auto ct-flex-center min-h-screen">
			<LoginForm />
		</div>
	);
};

export default page;
